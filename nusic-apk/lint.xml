<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <issue id="InvalidPackage">
        <!-- Lets assume that no classses from package java.rmi. are actually used -->
        <ignore path="**/jdom-1.0.jar" />
        <!-- logback-android is not going to use javax.mail see https://github.com/tony19/logback-android/issues/29 -->
        <ignore path="**/logback-android-core*.jar" />
        <!-- Roboguice contains the javax.inject package itself-->
        <ignore path="**/roboguice-*.jar" />
    </issue>
    <!-- For some reason this error breaks the build on jenkis with the unspecific message
        "Can't find API database; API check not performed"
        Just skip it for now-->
    <issue id="LintError" severity="warning" />
</lint>