apply plugin: "java"
compileJava {
    sourceCompatibility = nusicSourceCompatibility
    targetCompatibility = nusicTargetCompatibility
}

dependencies {
    // Test dependencies
    testCompile(
            libraries.junit,
            libraries.hamcrest,
            libraries.mockito
    )
}
