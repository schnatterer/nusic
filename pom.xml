<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>info.schnatterer.nusic</groupId>
	<artifactId>nusic-parent</artifactId>
	<version>1.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>nusic</name>

	<licenses>
		<license>
			<name>GNU General Public License 3.0</name>
			<url>http://www.gnu.org/copyleft/gpl.html</url>
		</license>
	</licenses>

	<scm>
		<connection>scm:git:ssh://github.com/schnatterer/nusic.git</connection>
		<developerConnection>scm:git:ssh://git@github.com/schnatterer/nusic.git</developerConnection>
		<url>https://github.com/schnatterer/nusic</url>
		<tag>HEAD</tag>
	</scm>

	<!-- When releasing, ignore musicbrainzws2-java -->
	<profiles>
		<profile>
			<id>release</id>
			<activation>
				<property>
					<!-- Value set by release:perform -->
					<name>performRelease</name>
					<value>true</value>
				</property>
			</activation>
			<modules>
				<module>nusic</module>
			</modules>
		</profile>
	</profiles>

	<modules>
		<module>nusic</module>
		<!-- TODO remove as soon as a proper version is published and nusic-app 
			does not reference this any more -->
		<module>musicbrainzws2-java</module>
	</modules>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-release-plugin</artifactId>
				<version>2.5.1</version>
				<configuration>
					<arguments>-Prelease</arguments>
					<!-- Make our tags look like e.g. "v.1.0.1" -->
					<tagNameFormat>v.@{project.version}</tagNameFormat>
					<!-- Same version to all submodules -->
					<autoVersionSubmodules>true</autoVersionSubmodules>
				</configuration>
			</plugin>
			<!-- Don't deploy on release -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<version>2.8.2</version>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
		</plugins>

	</build>
</project>